Role: Act as a Senior AI Architect and Lead Full-Stack Engineer specializing in Sovereign AI and Agentic GRC.

Objective: Implement the "Sentinel OS" codebase using industry-leading 2026 standards for security, scalability, and Object-Oriented Programming (OOP).

1. Architectural Requirements (OOP & Design Patterns):

Encapsulation: Create a SentinelBrain class in backend/ai.js to manage Gemini 3 API interactions. Use private methods for prompt construction and public methods for analyzeCompliance().

Singleton Pattern: Ensure the Google Generative AI client is initialized as a singleton to prevent multiple redundant connections.

Service-Oriented Architecture: Separate business logic (compliance reasoning) from the transport layer (Express routes). Use a ComplianceService class to orchestrate between the AI and the database.

2. Technical Stack & Integration:

AI Engine: Use the gemini-3-flash-preview model via @google/generative-ai. Enable the googleSearchRetrieval tool for real-time regulatory grounding.

Document Logic: Integrate the @foxitsoftware/foxit-pdf-sdk-node to create a DocumentProcessor class that converts PDFs to structured text before AI analysis.

Knowledge Base: Set up a SanityClient class to interface with Sanity.io for fetching structured "Source of Truth" regulatory schemas.

3. Security & Protocol Standards:

Zero-Trust API: Implement a SecurityMiddleware to validate all incoming requests. Use environment variables exclusively (process.env.GEMINI_API_KEY) and ensure no secrets are logged.

Input Sanitization: Use a robust validation layer (e.g., zod or joi) to sanitize all user-provided logs and regulatory text to prevent Prompt Injection and XSS.

Error Handling: Implement a Global Error Handler that catches asynchronous exceptions and returns standardized, secure JSON responses (no internal stack traces exposed).

Rate Limiting: Apply express-rate-limit to the /api/audit endpoint to prevent brute-force abuse.

4. Frontend Requirements (Generative UI):

React State Management: Use the useReducer or Context API to manage the "Audit Lifecycle" (Standby -> Processing -> Risk_Assessment -> Complete).

Enterprise UI: Build a high-fidelity, dark-mode "Sovereign Dashboard" using Tailwind CSS. Include a real-time "Risk Score" gauge and a "Reasoning Log" that displays the Agent's step-by-step logic.

5. Implementation Steps:

Initialize the backend server with Express and ESM modules.

Build the SentinelBrain class with a focus on "Context Grounding" (forcing the AI to cite sources).

Connect the frontend to the backend via a secure AbortController-enabled fetch request.

Generate a README.md that explains the "Vaulted IP" architecture and how to run the system.

Execute the plan in "Build Mode". Do not stop until the system is audit-ready.